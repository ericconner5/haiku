export class Haiku {
  constructor(line1, line2, line3) {
    this.line1 = line1;
    this.line2 = line2;
    this.line3 = line3;
  }

  instantiateHaiku() {
    // This is where we should set up Line1 = Line1, Line2 = Line2 -- but with actual values, and then assign them to new Haiku objects. Which can be returned to the user.

  }

}
